import 'package:flutter/material.dart';

Color onPrimaryColor = Colors.white;
TextStyle titleStyle = TextStyle(color: Colors.black);
EdgeInsets padding8 = EdgeInsets.all(10.0);
CircularProgressIndicator loadingWidget = CircularProgressIndicator(
  strokeWidth: 1.2,
  color: Colors.green,
);
CircularProgressIndicator loadingWidgetOnPrimaryColor =
    CircularProgressIndicator(
  strokeWidth: 1.2,
  color: Colors.white,
);
